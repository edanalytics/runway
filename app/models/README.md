# Models

This package holds data models shared between other packages. In particular:

- Entity TypeScript interfaces
- Entity DTOs (data transfer objects) configured with class-validator and class-transformer
- Other ad hoc enums and types
