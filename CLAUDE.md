@AGENTS.md

## Claude-specific Notes

- When making multi-file changes, read all target files before editing any of them
- FE TypeScript checks: `npm run fe:typecheck` (from `app/`)
- Don't commit unless explicitly asked; don't push unless explicitly asked
